import React from "react";
import { View, Text, TextInput, StyleSheet, SafeAreaView } from "react-native";

export const User = ({ navigation }) => {


    return (
        <SafeAreaView>

            <Text>
                User Setting will go here!
            </Text>
        </SafeAreaView>
    )
} 